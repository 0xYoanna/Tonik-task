# Copy this file to .env and paste your key.
#
#   cp .env.example .env
#
# Get a key: https://console.anthropic.com → API keys
# Restart the dev server after editing — env is read at startup.
#
# Without a key the app still runs, on the local keyword detector.
ANTHROPIC_API_KEY=sk-ant-...
